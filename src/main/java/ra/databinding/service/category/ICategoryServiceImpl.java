package ra.databinding.service.category;import org.springframework.stereotype.Service;import ra.databinding.dao.CategoriesDao;import ra.databinding.model.dto.request.CategoryDto;import ra.databinding.model.entity.Category;import java.util.List;@Servicepublic class ICategoryServiceImpl implements ICategoryService {    private final CategoriesDao categoriesDao;    public ICategoryServiceImpl() {        this.categoriesDao = new CategoriesDao();    }    @Override    public List<Category> findAll() {        return categoriesDao.findAll();    }    @Override    public Category findById(Integer id) {        return categoriesDao.findById(id);    }    @Override    public void save(Category category) {        categoriesDao.save(category);    }    @Override    public void remove(Integer id) {        categoriesDao.remove(id);    }//    @Override//    public void changeStatus(Integer id) {//        Category category = categoriesDao.findById(id);//        if (category != null) {                            // Kiểm tra xem category có tồn tại không//            boolean newStatus = !category.isStatus();      // Đảo ngược trạng thái hiện tại//            categoriesDao.changeStatus(id, newStatus);     // Thực hiện thay đổi trạng thái//        } else {//            throw new IllegalArgumentException(" không tim thấy id" + id);//        }//    }    @Override    public void changeStatus(Integer id) {    }    @Override    public void saveCategoryDto(CategoryDto categoryDto) {        Category category = new Category();        category.setName(categoryDto.getName());        save(category);    }    @Override    public void upDate(Category category) {        categoriesDao.update(category);    }    public void delete(Integer id){        categoriesDao.remove(id);    }}