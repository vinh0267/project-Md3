package ra.databinding.until;import ra.databinding.model.entity.Category;import java.sql.*;public class ConnectJava {    private static final String DRIVER = "com.mysql.cj.jdbc.Driver";    private static final String URL = "jdbc:mysql://localhost:3306/data_binding?createDatabaseIfNotExist=true";    private static final String USER = "root";    private static final String PASS = "vinhdang026@gmail.com";    public static void main(String[] args) {        Connection conn = openConnection();    }    // mở kết nối    public static Connection openConnection(){        Connection conn = null;        // B1 đăng kí driver        try {            Class.forName(DRIVER);            //B2 ở kết nối            conn = DriverManager.getConnection(URL,USER,PASS);            return conn;        } catch (ClassNotFoundException | SQLException e) {            throw new RuntimeException(e);        }    }    public static void closeConnection(Connection conn){        try {            if (!conn.isClosed()){                conn.close();            }        } catch (SQLException e) {            throw new RuntimeException(e);        }    }}